



не вижу в журнале группы Админов сброса сессий 



запись в журнале о назначение амина недостаточно информативна. Должно быть: КТО, для какой ГРУППЫ Назначил администратора КОГО 



подумай (без реализации) как можно автоматизировать создание топиков для группы по имени группы. Учти что имя группы скорее всего на кириллице и содержит недопустимые символы.



есть идея создавать админов групп автоматически в виде ИМЯ_ТОПИКА@admins . Тогда все админы созданные супером окажутся в одной фиктивной группе, что достаточно удобно. 


нарушена логика управления и отчетов ESP в брокер. вместо relay/GROUR_NAME/status сейчас логичнее публиковат статусы только изменений в device/UID/status в виде {[{"group":ИМЯ, "state":"on"}],"ts":1772794791 }. При старте логично публиковать весь массив групп/реле.
Нужно править и бэк и скетч. также меняем и команды от бэка к esp. esp подписывается на device/UID/cmd и получает команду {"group":ИМЯ,"action":"pulse","duration":500}. Пока только оценка без реализации.

начинаем прорабатывать вопрос подключения sim800 к ESP32 (ТОЧНЕЕ использования TT-Go Call ) для исполнительного устройства. Нужно проработать механизм получения списка авторизованных телефонов при старте esp, синхронизации добавления и удаления номеров(пользователей) в процессе работы, про обработки входящих звонков и СМС (библиотека своя, готова). Входящие звонки вызывают срабатывание реле 0. СМС разбирается по содержимому: индекс и/или именя группы  вызывает триггер соответствующей группы/реле. Реакция на звонки/СМС должна быть только при наличии Пользователя в соответствующей Группе. ESP должно уведомить брокер об срабатывании триггера от пользователя. В случае отсутствия связи с брокером на esp должен вестись локальный журнал для синхронизации журнала при появлении интернета. 






